#/usr/bin/env bash


renewTmux(){
    tmux new-session -s session1 -n editor -d 'nvim ; $SHELL'
    tmux new-window -t session1:2 -n terminal
    tmux select-window -t session1:2
    tmux attach-session -t session1
}

if [ -z "$TMUX" ]; then
  tmux attach -t session1 || renewTmux
fi
